##!/bin/bash
#while getopts "b:t:" opt; do
#  case ${opt} in
#    b ) server=$OPTARG ;;
#    t ) topic=$OPTARG ;;
#    ? ) echo "Usage: $0 [-a] [-b]" 1>&2
#        exit 1 ;;
#  esac
#done
#printf "springboot "
##exec kafka-console-consumer-interface.sh --bootstrap-server $server --topic $topic
